# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

[versioneer]
VCS = git
style = pep440
versionfile_source = src/spec_ibride/_version.py
versionfile_build = src/spec_ibride/_version.py
tag_prefix = v
parentdir_prefix = ibride-

[pep8]
max_line_length = 120
